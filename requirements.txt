Django==5.2.8
psycopg[binary]==3.2.3
python-decouple==3.8
Pillow==11.0.0
django-widget-tweaks==1.5.0
whitenoise==6.8.2
gunicorn==23.0.0
dj-database-url==2.3.0

# REST API for the mobile app
djangorestframework==3.15.2
django-cors-headers==4.6.0

# Firebase Cloud Messaging (push notifications)
firebase-admin==6.6.0

# Cloudinary media storage.
#
# Receipts and avatars must survive a redeploy. Hosts like Render and Railway
# wipe the local filesystem on every restart, so anything written to
# MEDIA_ROOT there is gone - and a receipt is the evidence behind a payment.
#
# Only activated when CLOUDINARY_* credentials are present in the environment
# (see config/settings.py), so local development still works without an
# account and without these two packages ever being reached.
cloudinary==1.41.0
django-cloudinary-storage==0.3.0

/*
 * Retired.
 *
 * The web app used to switch between a light and a dark theme here. It is
 * light only now - a white, professional look to match the phone app - and
 * the three pages that loaded this file no longer do (see base.html, where
 * <html data-bs-theme="light"> is fixed). Nothing includes this file; it is
 * kept empty only so an old cached page that still asks for it gets a 200
 * instead of a 404 in the console. Safe to delete.
 */

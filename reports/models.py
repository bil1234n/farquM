"""
The reports app is a pure read/aggregation layer - it owns no tables.

Every figure shown here is computed from the sales, inventory and credit
ledgers at request time, so a report can never disagree with the records
it summarises.
"""
